In these we cover basic css property for default browser spaces.

1) There is one question which is in comment inside body tag.

2) Use "styles.css" file for css related. 

3) Run ".html" file to check the output.